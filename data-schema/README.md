# Data Schema

This folder documents the expected folder structure for the data files used in this project.

The `data-schema` directory does not contain the actual data. Instead, it provides a lightweight reference layout showing where each required data file should be placed.

Use this structure as a guide when preparing the local data directory before running the project.

